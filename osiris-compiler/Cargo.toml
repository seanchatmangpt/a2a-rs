[package]
name = "osiris-compiler"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
authors = ["Emil Lindfors <emil@lindfors.no>"]
description = "Deterministic compiler for Osiris protocol operations"
license = "MIT"
repository = "https://github.com/emillindfors/a2a-rs"
keywords = ["compiler", "deterministic", "osiris"]
categories = ["compilers"]

[dependencies]
# Core dependencies
a2a-rs = { path = "../a2a-rs", default-features = false, features = ["client", "http-client", "server", "http-server", "tracing"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
thiserror = "1.0"
uuid = { version = "1.4", features = ["v4", "serde"] }
bon = "2.3"

# Async foundation
tokio = { version = "1.32", features = ["rt", "rt-multi-thread", "macros", "net", "io-util", "sync", "time"] }
async-trait = "0.1"
futures = "0.3"
serde_bytes = "0.11"

# Server
axum = "0.8"

# AI/LLM
genai = "0.1"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

# Cryptography
sha2 = "0.10"
base64 = "0.22"
hex = "0.4"

# Cloud services (optional)
google-cloudkms1 = { version = "5.0", optional = true }
google-firestore1 = { version = "5.0", optional = true }
google-cloud-storage = { version = "0.18", optional = true }
google-gmail1 = { version = "7.0", optional = true }
google-calendar3 = { version = "7.0", optional = true }
google-drive3 = { version = "7.0", optional = true }
hyper = { version = "1.0", optional = true }
hyper-rustls = { version = "0.27", optional = true }
yup-oauth2 = { version = "9.0", optional = true }

[features]
default = ["tokio-runtime", "tracing"]
tokio-runtime = []
tracing = []
timestamps = []
builders = []
kms = ["google-cloudkms1", "hyper", "hyper-rustls", "yup-oauth2"]
firestore = ["google-firestore1", "hyper", "hyper-rustls", "yup-oauth2"]
storage = []
gcs = ["google-cloud-storage"]
workspace-publisher = ["google-gmail1", "google-calendar3", "google-drive3", "hyper", "hyper-rustls", "yup-oauth2"]

[dev-dependencies]
proptest = "1.4"
proptest-derive = "0.5"

[[bin]]
name = "osiris-compiler"
path = "src/main.rs"

[lib]
name = "osiris_compiler"
path = "src/lib.rs"
